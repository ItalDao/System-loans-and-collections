<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>
<body>
    <div class="container-fluid">
        <div class="row flex-nowrap">
            <%- include('../partials/sidebar') %>

            <div class="col py-4">
                <div class="container-lg">
                    <!-- HEADER -->
                    <div class="d-flex justify-content-between align-items-center mb-5 pb-3 border-bottom border-light">
                        <div>
                            <h1 class="mb-1"><i class="bi bi-gem" style="color: #f59e0b;"></i> Empeños y Garantías</h1>
                            <p class="text-muted mb-0">Administra artículos en custodia y deudas prendarias</p>
                        </div>
                        <a href="/empenos/crear" class="btn btn-warning text-white fw-bold">
                            <i class="bi bi-box-seam-fill"></i> Nuevo Empeño
                        </a>
                    </div>

                    <% if(mensajeExito && mensajeExito.length > 0) { %>
                        <div class="d-none" id="flashMessageSuccess"><%= mensajeExito %></div>
                    <% } %>

                    <% if (empenos.length > 0) { %>
                        <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-3 g-4 mb-5">
                            <% empenos.forEach((e, index) => { %>
                                <div class="col" style="animation: fadeIn 0.5s ease-out <%= index * 0.05 %>s backwards;">
                                    <div class="card card-box h-100 border-0 hover-lift">
                                        <!-- Imagen -->
                                        <div style="height: 220px; overflow: hidden;" class="d-flex align-items-center justify-content-center position-relative" style="background: rgba(76, 196, 182, 0.15);">
                                            <% if(e.imagen) { %>
                                                <img src="/uploads/<%= e.imagen %>" class="card-img-top" style="height: 100%; width: 100%; object-fit: cover;">
                                            <% } else { %>
                                                <div class="text-center">
                                                    <i class="bi bi-camera-off" style="font-size: 2.5rem; color: rgba(76, 196, 182, 0.5);"></i>
                                                    <p class="text-muted mt-2 mb-0">Sin fotografía</p>
                                                </div>
                                            <% } %>
                                            <!-- Estado Badge -->
                                            <div style="position: absolute; top: 10px; right: 10px;">
                                                <% if(e.estado === 'en_custodia') { %>
                                                    <span class="badge-estado badge-activo">En Custodia</span>
                                                <% } else { %>
                                                    <span class="badge-estado" style="background: #e5e7eb; color: #374151; border-left-color: #6b7280;">Devuelto</span>
                                                <% } %>
                                            </div>
                                        </div>

                                        <div class="card-body d-flex flex-column">
                                            <!-- Nombre Artículo -->
                                            <h5 class="card-title fw-700 mb-2" style="color: #0f1419;">
                                                <%= e.nombre_articulo %>
                                            </h5>
                                            
                                            <!-- Descripción -->
                                            <p class="card-text text-muted small mb-3" style="flex-grow: 1; max-height: 40px; overflow: hidden; text-overflow: ellipsis;">
                                                <%= e.descripcion %>
                                            </p>

                                            <!-- Valores -->
                                            <div class="row g-2 mb-3 pb-3 border-bottom border-light">
                                                <div class="col-6">
                                                    <small class="text-muted d-block">Tasación</small>
                                                    <span class="dinero-highlight"><%= empresa.moneda %><%= parseFloat(e.valor_tasacion).toFixed(2) %></span>
                                                </div>
                                                <div class="col-6">
                                                    <small class="text-muted d-block">Monto Prestado</small>
                                                    <span class="dinero-highlight text-info"><%= empresa.moneda %><%= parseFloat(e.monto_prestado).toFixed(2) %></span>
                                                </div>
                                            </div>

                                            <!-- Fecha Límite -->
                                            <small class="text-muted d-block mb-3">
                                                <i class="bi bi-calendar3"></i> 
                                                Límite: <strong><%= new Date(e.fecha_limite).toLocaleDateString('es-ES', {year: 'numeric', month: 'short', day: 'numeric'}) %></strong>
                                            </small>

                                            <!-- Acciones -->
                                            <div class="d-flex flex-column gap-2">
                                                <% if(e.estado === 'en_custodia') { %>
                                                    <a href="/empenos/liberar/<%= e.id %>" class="btn btn-success btn-sm" onclick="return confirm('¿Confirmar devolución del artículo?');">
                                                        <i class="bi bi-arrow-return-left"></i> Devolver
                                                    </a>
                                                <% } else { %>
                                                    <span class="badge bg-info text-white w-100 text-center py-2">Devuelto</span>
                                                <% } %>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    <% } else { %>
                        <div class="card card-box border-0 empty-state">
                            <div class="empty-state-icon">
                                <i class="bi bi-box"></i>
                            </div>
                            <h4 class="empty-state-heading">No hay empeños registrados</h4>
                            <p class="empty-state-text">Comienza registrando tu primer artículo en custodia</p>
                            <a href="/empenos/crear" class="btn btn-warning text-dark">
                                <i class="bi bi-plus-circle-fill"></i> Nuevo Empeño
                            </a>
                        </div>
                    <% } %>

                </div>
            </div>
        </div>
    </div>

    <style>
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fw-700 {
            font-weight: 700;
        }

        .empty-state {
            padding: 60px 20px;
            text-align: center;
        }

        .empty-state-icon {
            font-size: 4rem;
            color: #cbd5e1;
            margin-bottom: 20px;
        }

        .empty-state-heading {
            color: #64748b;
            margin-bottom: 10px;
        }

        .empty-state-text {
            color: #94a3b8;
            margin-bottom: 20px;
        }
    </style>

<%- include('../partials/footer') %>